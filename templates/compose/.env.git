# __SVC__ — non-secret config, committed plaintext (Arcane's git layer). — compose.md
# Secrets NEVER go here. They live only in compose/__SVC__/.env.sops (sops+age), created with:
#   sops --encrypt --input-type dotenv --output-type dotenv \
#     --filename-override compose/__SVC__/.env.sops <plaintext-project.env> > compose/__SVC__/.env.sops
# Non-secret defaults below, one KEY=value per line.
TZ=Asia/Karachi
