---
date: 2026-09-01
kind: session
title: nightly 2026-09-01 1735 rerun
tier_touched: [T1]
grants: []
refs: ["branch inventory/2026-09-01-1735", "vm-skynet-ops", "server-proxmox-core", "server-proxmox-network", "docker-dmz"]
---

# 2026-09-01 · session · nightly 2026-09-01 1735 rerun

## What happened

Created an isolated worktree at `b3cde472116b96db1398ff06ef93a9d66a477ad0`, the fetched
`origin/main`, because the primary checkout contained unrelated modified and untracked journal
files. Created branch `inventory/2026-09-01-1735` in the isolated worktree.

Ran `./bin/ops collect` at 17:36 PKT. The core and network Proxmox collectors wrote JSON. The PBS
collector printed `no creds yet (/opt/skynet-ops/secrets/pbs.env) — collector idle until A2/A4`.
Docker, DNS, firewall, and network-gear collectors wrote JSON. The firewall collector reported
mirror HEAD `a610af0` dated 2026-09-01 17:20:32 +0500 and counted 41 aliases, 29 rules, and 6
reservations. The network-gear collector reported three devices and one site. `bin/ops collect`
also ran `scripts/render-docs.sh` and rendered at `2026-09-01T17:36:06+05:00`.

Ran `./scripts/envsync.sh`. It printed `skip aiometadata: no project.env on host` and `skip
aiostreams: no project.env on host`; no `.env.sops` file changed.

Searched `/home/aliammar/.ssh` for `*-cert.pub`; no path was returned, and
`/home/aliammar/.ssh/certs` did not exist. No root grant was active, no remote sshd log was read,
and `inventory/grant-audit.json` was not written.

Compared live inventory with `origin/main`. Proxmox identity/state projections were identical.
VMID 9090 (`vm-skynet-ops`) was running with uptime 13,894 seconds, while the 12:50 baseline
recorded 334,857 seconds. VMID 730 was running with uptime 18,224 seconds versus 1,074 seconds at
12:50. Every other running guest's uptime advanced. Docker contained the same 18 container IDs,
images, states, and health states; all 18 were running and healthy. The Technitium primary-zone
serial changed 286 to 287 and the secondary-zone serial changed 2026083102 to 2026090100. The
firewall JSON differed only in collection time. Network devices stayed connected on the same
firmware; Main Switch client count changed 18 to 19.

Rewrote `docs/generated/05-state-of-the-lab.md` with the 17:36 evidence and the VMID 9090 restart
called out. Then ran the deterministic digest and context-map renderers after creating this entry.

## Actions & outcomes

- `git fetch origin --prune` → resolved `origin/main` to `b3cde47`.
- `git worktree add -b inventory/2026-09-01-1735 ... origin/main` → isolated the nightly from the dirty primary checkout.
- `./bin/ops collect` → all configured T1 collectors ran; PBS skipped for absent credential.
- `./scripts/envsync.sh` → both tracked projects lacked host-side `project.env`; no encrypted env change.
- Local signed-certificate check → no active root grant; grant-audit harvest skipped.
- Inventory comparison → found the VMID 9090 restart and otherwise stable guest/container identity and health.
- Human narrative rewrite → recorded current state, mixed-age baseline, restart anomaly, and unverified backup evidence.

## Graveyard — tried & abandoned

- `./bin/new journal session "nightly 2026-09-01"` → abandoned because
  `journal/2026/2026-09-01-session-nightly-2026-09-01.md` already exists and journal episodes are
  append-only; stamped the distinct `nightly 2026-09-01 1735 rerun` episode instead.
- Initial jq query using `.guests[]` on Proxmox JSON → abandoned because the collector schema uses
  `.resources[]`; reran the comparison against `.resources`.

## Follow-ups / open threads

- Establish whether the VMID 9090 restart between the 12:50 and 17:36 snapshots was planned.
- Recent PBS snapshots and the L5 Google Drive mirror remain unverified because the PBS credential was absent and no root grant was active.
- Confirm that core VMIDs 101, 231, 999, and 9091 were intentionally removed.
- Resolve ownership of `10.10.100.35` before any destruction of stopped CT 1035.
- CT 526 remains running and unmapped in DNS/reservations.
