---
date: 2026-09-02
kind: session          # session | incident | decision
title: nightly 2026-09-02
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [runbooks/nightly.md, inventory/2026-09-02-0339, server-proxmox-core, server-proxmox-network, docker-dmz, opnsense, technitium, omada]
---

# 2026-09-02 · session · nightly 2026-09-02

## What happened

- Fetched `origin/main` and created branch `inventory/2026-09-02-0339` at commit `edfb521` in a
  separate worktree because the original checkout contained an unrelated modification to
  `tofu/dns-aliammar-net.tf`.
- Ran `./bin/ops collect` at 03:39 PKT. It wrote fresh Proxmox core/network, Docker DMZ,
  Technitium DNS, OPNsense firewall/live state, Omada, route, and certificate inventories. The
  command exited 0.
- The PBS collector printed `no creds yet (/opt/skynet-ops/secrets/pbs.env) — collector idle until
  A2/A4`; it did not refresh backup evidence.
- Searched `/home/aliammar/.ssh` for `*-cert.pub` and found none. No root grant was active, no
  granted-root session was used, and `inventory/grant-audit.json` was not harvested.
- Ran `./scripts/envsync.sh`. It printed `skip aiometadata: no project.env on host` and `skip
  aiostreams: no project.env on host`, then exited 1. No `compose/*/.env.sops` file changed.
- Ran `./scripts/render-docs.sh`, `./scripts/render-digest.sh`, and
  `./scripts/render-context-map.sh`; all three exited 0. Rewrote
  `docs/generated/05-state-of-the-lab.md` from the fresh inventory and the diff against
  `origin/main`.
- Against `origin/main`, Technitium's `tdns.home.aliammar.net` primary zone moved from SOA serial
  287 to 291 and gained `tofu-test.tdns.home.aliammar.net A 192.0.2.1` with comment `Managed by
  terraform`; related DNSSEC records changed. The secondary zone serial moved from 2026090101 to
  2026090102.
- Both Proxmox nodes remained online. All listed non-template guests remained running; VM 9000
  remained stopped. All 18 Docker containers remained running and healthy. OPNsense remained at
  39 aliases, 27 rules, and 1 reservation, with 25 declared hosts live and 1 not responding.
- Omada remained three devices up and 30 clients total: main switch 20, Mom's AP 5, Ali's AP 5.
  All seven configured TLS probes responded.

## Actions & outcomes

- `bin/ops collect` → fresh T1 inventory from every configured collector except credential-gated
  PBS.
- `scripts/envsync.sh` → no encrypted env changes; nonzero exit after two missing `project.env`
  messages.
- deterministic renderers → generated factual docs, agent digest, and context map successfully.
- narrative rewrite → recorded the DNS test record, stable workload/firewall state, and evidence
  gaps without claiming provenance.
- root-grant check → no certificate present; audit harvest skipped without contacting hosts as root.

## Graveyard — tried & abandoned

- PBS inventory refresh → unavailable because `/opt/skynet-ops/secrets/pbs.env` was absent; no
  credential was requested or created in report-only scope.
- `aiometadata` and `aiostreams` env synchronization → skipped by `envsync.sh` because neither
  host project had `project.env`; no attempt was made to invent or retrieve secret material.
- Root-grant audit harvest → skipped because no local SSH certificate existed and no grant was
  active.

## Follow-ups / open threads

- Confirm the intended lifetime of `tofu-test.tdns.home.aliammar.net → 192.0.2.1` through the
  reviewed OpenTofu workflow.
- Confirm whether missing `project.env` files for `aiometadata` and `aiostreams` are intentional.
- PBS snapshot freshness, restic payloads, restore behavior, and the L5 Google Drive mirror remain
  unverified.
