---
date: 2026-08-30
kind: session          # session | incident | decision
title: nightly 2026-08-30
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [runbooks/nightly.md, "inventory/2026-08-30", "PR #113", "PR #115", SKY-008]
---

# 2026-08-30 · session · nightly 2026-08-30

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened
Ran the agent-path nightly (Claude, manual invocation) per `runbooks/nightly.md`. Started on
branch `inventory/2026-08-29` (checked out from a prior session), untracked file
`journal/2026/2026-08-28-session-seed-jikan-anime-index-for-aiometadata.md` present in the
working tree (someone's prior session note, left alone, not part of this run).

Checked git state before starting: `main` was at `e3be142` locally but `origin/main` was at
`ecd80b2`, **12 commits ahead** — `main` had not absorbed the last two nightly PRs (**#113**,
nightly 08-28, and **#115**, nightly 08-29 / `f9d63d9`), both still open. Also merged since:
`docs/system-design.md` additions, ADR 0005 (full agent control terminal goal), the finalized
`docs/conventions/naming.md`, and two new ideas-stage directives SKY-017 + SKY-018 (all via
PR #114, "netbox-vs-nautobot", merged earlier tonight/today by Ali).

Fast-forwarded local `main` to `origin/main` (`git merge --ff-only`), then branched
`inventory/2026-08-30` off the fast-forwarded `main`. This means tonight's diff-vs-`main` in the
PR spans 3 nights of inventory drift (08-27→08-30), not one — worked around by diffing separately
against `f9d63d9` (PR #115's tip, the actual last-observed state) to get an accurate "since last
night" read for the narrative.

`bin/ops collect` ran clean: proxmox-core, proxmox-network, docker-docker-dmz, dns-zones,
firewall all wrote. PBS collector stayed idle (no creds at `/opt/skynet-ops/secrets/pbs.env`,
expected). `scripts/envsync.sh` found no `project.env` changes on any of the 10 known projects.
`scripts/render-docs.sh`, `render-digest.sh`, `render-context-map.sh` all ran clean, stamped
`2026-08-30T03:44:1x+05:00`.

No active root grant: `~/.ssh/certs/` doesn't exist, `/home/ali/.config/skynet-ops/` doesn't
exist (only `~/.config/skynet-ops/ops.env`, a home-manager-managed symlink, unrelated to grants).
Skipped the grant-audit step (runbook step 5).

Diffed `proxmox-core.json` / `proxmox-network.json` / `docker-docker-dmz.json` / `dns-zones.json`
/ `firewall.json` against `git show f9d63d9:...` (last night's actual tip, not stale `main`) with
a python3 walk + filtered `git diff`, to separate real state changes from JSON key-order noise
(the proxmox collector doesn't emit stable key ordering — pure diff noise across every resource,
confirmed harmless).

## Actions & outcomes
- `git fetch origin main` + `git merge --ff-only origin/main` → local `main` caught up 12 commits
  to `ecd80b2`.
- `git checkout -b inventory/2026-08-30` (off the fast-forwarded `main`).
- `bin/ops collect` → refreshed all 5 inventory files, ran `render-docs.sh` once inline.
- `./scripts/envsync.sh` → "no env changes".
- `./scripts/render-docs.sh`, `render-digest.sh`, `render-context-map.sh` (explicit re-runs) →
  all wrote clean, no errors.
- Diff vs `f9d63d9` (last night, 2026-08-29 03:30 +05) found:
  - VMID 240 (`lxc-proxmox-backup-server`, `ops-managed`): still `stopped` — unchanged from last
    night, now 4 consecutive nights stopped (last running 08-27). Still unexplained, still not
    acted on (T2 guest-power action, out of report-only scope; not touched by any tofu session).
  - VMID 999 (legacy `vm-skynet-ops`): still `running` — unchanged from last night, still
    unexplained, not worsening.
  - `vm-docker-dmz` containers: all `Status` moved from `Up 11 hours` to `Up 35 hours` — exactly
    +24h, confirming no second restart happened; the 08-28 ~16:30 event was one-time, not
    recurring.
  - `inventory/firewall/firewall.json`: byte-identical content (41 aliases, 29 rules) — only the
    collector timestamp moved. No new OPNsense edits since 08-29.
  - `inventory/dns-zones.json`: routine SOA serial advance only
    (`tdns.home.aliammar.net` 2026082801 → 2026082901), cert `lastUsedOn` timestamps moved.
  - No adds/removes in the docker container list; no new/removed Proxmox guests on either node.
- Rewrote `docs/generated/05-state-of-the-lab.md` — dated 2026-08-30, framed around both diffs
  (vs `main` for the PR's literal content, vs `f9d63d9` for an accurate "since last night" read),
  flagged the PBS 4th-night-stopped state and the growing nightly-PR merge backlog (#113, #115
  both still open) as the two things most worth Ali's attention.
- `bin/new journal session "nightly 2026-08-30"` → this entry.

## Graveyard — tried & abandoned
- Tried computing "what changed since last night" as a straight `git diff main` first — abandoned
  because `main` is 2 nightly PRs stale, so that diff conflates 3 nights of drift and would have
  mis-attributed 08-27→08-29 changes (e.g. the OPNsense alias edits already reported last night)
  as new tonight. Switched to diffing against `f9d63d9` directly for the narrative's "since last
  night" section, keeping the `main`-relative diff only as the PR's literal changed-files list.

## Follow-ups / open threads
- **PBS (VMID 240) — 4 nights stopped as of tonight.** Needs a human decision: deliberate
  maintenance or an unplanned outage quietly starving the L5 PBS→Drive backup layer. Escalating
  the framing tonight since it's no longer a one-off flag.
- **Nightly PR backlog**: #113 (08-28) and #115 (08-29) are both open, unmerged, report-only/
  generated-only — should be safe to merge and would stop `main` drifting further from the
  inventory's actual state.
- **VMID 999 still running, unexplained** — third observation in a row, probably benign
  (Ali comparing something against the old pre-NixOS box?) but still unconfirmed.
- From the 2026-08-28 entity-model decision journal entry (carried via the agent digest, not
  independently re-verified tonight): `10.10.100.35`'s ownership needs resolving before CT 1035
  (caddy-dmz, stopped) gets destroyed — nine published apps depend on it; CT 526 (UniFi
  controller) is running and unmapped in DNS/reservations; `arcane-manager`'s GitOps-exception
  status is still Ali's call.
- SKY-008 P3 (DNS provider + declarative LXC import) still not started (per
  `planning/projects/SKY-008-*.md` status log — Phase 1+2 done both nodes, Phase 3 open).
