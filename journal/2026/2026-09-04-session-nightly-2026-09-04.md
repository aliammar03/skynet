---
date: 2026-09-04
kind: session          # session | incident | decision
title: nightly 2026-09-04
tier_touched: [T1]
grants: []
refs: [runbooks/nightly.md, inventory/2026-09-04-0342, server-proxmox-core, server-proxmox-network, vm-docker-dmz]
---

# 2026-09-04 · session · nightly 2026-09-04

## What happened

- Fetched `origin/main` at `80b86cf31a604abb5f328c8698c1e479eb34aad7` and created
  `inventory/2026-09-04-0342` from it. The worktree was clean before switching branches.
- Ran `bin/ops collect` at approximately 03:43 PKT. Both Proxmox collectors wrote inventory;
  PBS reported 1 datastore and 122 snapshots; Docker, DNS, live OPNsense, Omada, routes, and TLS
  collectors all wrote inventory. `bin/ops collect` exited 0.
- Ran `scripts/envsync.sh`. It printed `skip aiometadata: no project.env on host` and
  `skip aiostreams: no project.env on host`, then exited 1. No `compose/*/.env.sops` file changed.
- Checked the documented local SSH certificate location. `$HOME/.ssh/certs` did not exist at
  2026-09-04T03:43:17+05:00, so there was no active root grant to use and no host audit harvest ran.
- Ran `scripts/render-docs.sh`; it exited 0 and rendered at 2026-09-04T03:43:24+05:00. Rewrote
  `docs/generated/05-state-of-the-lab.md` from the fresh inventory and the diff against
  `origin/main`.
- Against `origin/main`, PBS snapshot count moved 111 → 122. The core 03:30 and network 02:00
  backup jobs both reported `OK` on 2026-09-04. Latest-snapshot `verify_state: null` count moved
  1 → 11: core CT 731/751; core VMs 10015/9000/9090; network CT 525/635/730/750/837; network VM
  5001. The collection happened minutes after the core job.
- Both Proxmox nodes were online. Every non-template guest was running; template VM 9000 was
  stopped. Docker returned 18 containers, all with `State=running` and a healthy status.
- OPNsense live inventory remained at 39 aliases, 27 rules, and 1 reservation. Presence moved
  24 live / 2 silent → 22 live / 4 silent. The silent IPs were `10.10.10.55`, `10.10.10.60`,
  `10.10.10.65`, and `10.10.80.37`.
- Omada returned 3 connected devices. Client counts were switch 17, Moms AP 5, Ali's AP 3; total
  25. TLS probing reached all 7 configured endpoints.

## Actions & outcomes

- `git fetch origin main --prune` → fetched the latest base; no existing local or remote branch
  named `inventory/2026-09-04-0342` was found.
- `bin/ops collect` → refreshed all configured T1 inventory and invoked its built-in docs render.
- `scripts/envsync.sh` → skipped two absent `project.env` files, exited 1, staged no env changes.
- root-grant presence check → no certificate directory, audit skipped, grants remained `[]`.
- `scripts/render-docs.sh` → deterministic factual pages refreshed successfully.
- agent narrative rewrite → recorded the 122-snapshot state, 11 pending latest verifications,
  four silent declared IPs, healthy guest/container state, and unchanged autonomy boundary.

## Graveyard — tried & abandoned

- Harvesting host sshd KeyIDs → not attempted because no local grant certificate directory existed;
  acquiring or using a new root grant is outside this report-only pass.
- Treating all 11 null latest verification states as failed backups → abandoned because both jobs
  reported `OK`, ten groups had verified prior latest snapshots in `origin/main`, and the new
  snapshots landed immediately before collection. The raw null states remain recorded as pending.
- Continuing envsync after the second skip → the script exited 1 on its existing behavior; no
  alternate secret path was attempted and no plaintext was inspected.

## Follow-ups / open threads

- Check whether the ten newly created PBS snapshots acquire `verify_state: ok` after the verifier
  runs; VM 10015 remains the pre-existing latest-snapshot verification gap.
- Exercise an end-to-end restore; this pass did not test PBS restore, restic, or the L5 Drive mirror.
- Decide whether missing `project.env` for `aiometadata` and `aiostreams` is intentional and whether
  `scripts/envsync.sh` should exit 1 after those skips.
- Confirm whether `10.10.80.37` should answer continuously. The three silent `10.10.10.*` values
  are members of `ROLE_ADMIN_CLIENTS` and may be offline client slots.
