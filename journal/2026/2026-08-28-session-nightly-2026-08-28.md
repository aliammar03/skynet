---
date: 2026-08-28
kind: session          # session | incident | decision
title: nightly 2026-08-28
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [runbooks/nightly.md, "inventory/2026-08-28", SKY-008]
---

# 2026-08-28 · session · nightly 2026-08-28

## What happened
Ran the agent-path nightly (Claude, manual invocation, not the systemd timer) per
`runbooks/nightly.md`. Session started on `main`, working tree clean, HEAD `e3be142` (merge of
PR #111 inventory/2026-08-27 and PR #112 SKY-008 P2, plus three follow-up docs commits
`24997c1`/`1a7cde5`/`b97728d` scoping P3). Checked out fresh `inventory/2026-08-28` off that HEAD.

`bin/ops collect` ran all five collectors: proxmox-core, proxmox-network, docker-docker-dmz,
dns-zones, firewall. PBS collector stayed idle (no creds at `/opt/skynet-ops/secrets/pbs.env`,
expected — A2/A4 territory). `scripts/envsync.sh` found no `project.env` changes across all 10
known projects — "no env changes", "no project.env on host" for each (secrets live on
vm-docker-dmz, not here). `scripts/render-docs.sh`, `scripts/render-digest.sh`,
`scripts/render-context-map.sh` all ran clean.

No active root grant this session — checked `~/.ssh/certs/` (doesn't exist) and found no
`*-cert.pub` files anywhere under `~/.ssh`. Skipped the grant-audit step per runbook step 5
("skip if no root").

Diffed against `main` (15 files, +500/-493 net, but almost entirely JSON key-order churn +
timestamp/uptime bumps — see below). Since last night's inventory/2026-08-27 PR was merged, the
big event was SKY-008 Phase 2 landing end-to-end: PR #112 merged tofu VM/CT lifecycle proven on
**both** nodes (core: VM lifecycle round-trip; network: extended standalone + LXC clone
round-trip), plus three follow-up docs commits scoping Phase 3 (DNS via Technitium + LXC import)
and fixing an access-control gap (`/vms` `TofuVmConfig` binding shadows node-wide `VM.Audit` at
`/vms/<id>` — token was only listing pooled guests; added `VM.Audit` to `TofuVmConfig` for full
node-wide read parity on both nodes).

## Actions & outcomes
- `git checkout -b inventory/2026-08-28` off `origin/main` (`e3be142`) → clean tree.
- `./bin/ops collect` → wrote `inventory/proxmox-core.json`, `proxmox-network.json`,
  `docker-docker-dmz.json`, `dns-zones.json`, `firewall/firewall.json` (aliases=41, rules=29,
  reservations=6 — unchanged counts from last night).
- `./scripts/envsync.sh` → no changes (all 10 projects skip, expected).
- `./scripts/render-docs.sh`, `render-digest.sh`, `render-context-map.sh` → all wrote
  successfully.
- Diff vs main (15 files):
  - **proxmox-core.json / proxmox-network.json**: almost entirely JSON *key reordering* (the
    Proxmox API returns object keys in non-deterministic order per call — same keys/values, just
    shuffled) plus routine `memhost` (RSS) fluctuation on running guests. No VMID appeared,
    disappeared, or changed state/pool/tags since last night. Confirms the VMID 9090/9091
    renumber from the 2026-08-26/27 episodes is stable.
  - **docker-docker-dmz.json**: only `collected` timestamp and per-container `RunningFor`/
    `Status` uptime counters advanced by one day each (e.g. "Up 3 days" → "Up 4 days"); one
    `Mounts` field's comma-separated order flipped (same two paths, reordered) — cosmetic, not a
    real mount change. All containers still `running`/`healthy`.
  - **dns-zones.json**: routine SOA serial advances (both zones bumped one day), `lastUsedOn`/
    `lastModified`/`expiry` timestamps moved forward — nothing anomalous.
  - **firewall/firewall.json**: only the `collected` timestamp changed — alias/rule counts
    identical to last night (41/29/6), so no OPNsense edits landed since the `PORT_WEB` /
    `HOST_SKYNET_OPS` changes already flagged in the 2026-08-27 entry. `PORT_WEB`'s description
    regression is still unfixed (T3, out of this agent's write scope; still just a flag for Ali).
  - **docs/generated/**: 06-agent-digest.md and 07-context-map.md picked up the new git-derived
    facts (SKY-008 now shows `2/3` not `1/3`; recent-episodes list now includes the two
    2026-08-27 SKY-008 sessions + last night's nightly). Other generated pages only changed their
    `generated:` timestamp.

## Graveyard — tried & abandoned
— nothing abandoned (a clean pass; every step succeeded) —

## Follow-ups / open threads
- **JSON key-order churn is now a recurring nightly diff cost.** Two nights running,
  `proxmox-core.json`/`proxmox-network.json` show large line-diffs that are pure key reordering
  from the Proxmox API (not real state change) — makes the PR diff noisy and slower to eyeball
  for a real anomaly. Worth a follow-up: sort object keys before writing in
  `collect-proxmox.sh`, so future diffs are signal-only. Not fixed tonight — collectors aren't on
  the auto-approve list and this is a habitability nit, not an anomaly.
- `PORT_WEB` alias description is still the regressed generic placeholder ("Ports for WEB") as of
  tonight — carried over from 2026-08-27, unchanged; still flagging for Ali, not acting (T3).
- SKY-008 is now 2/3 phases done (P1 core skeleton, P2 VM/CT lifecycle both nodes); Phase 3 is
  scoped (DNS via Technitium + a zero-drift LXC import) per `24997c1` but not started.
- The `phase/sky-008-p2-throwaway-guest` stash mentioned in the 2026-08-27 entry is resolved —
  that branch's work landed via PR #112; no stash to worry about tonight.
