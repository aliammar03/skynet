---
date: 2026-09-01
kind: session
title: nightly 2026-09-01 2350
tier_touched: [T1]
grants: []
refs: [vm-skynet-ops, server-proxmox-core, server-proxmox-network, docker-dmz, OPNsense, Omada]
---

# 2026-09-01 · session · nightly 2026-09-01 2350

## What happened

- The existing checkout `/home/aliammar/skynet` was on
  `phase/sky-018-p5-routes-certs` with modified `inventory/firewall/firewall.json`, modified
  `inventory/opnsense.json`, and one untracked 2026-08-30 journal file. Those files were not touched.
- `git fetch origin main --prune` completed. A separate worktree was created from commit `a7968ec`
  (`origin/main`) on branch `inventory/2026-09-01-2350`.
- `bin/ops collect` ran at 23:51 PKT. It wrote Proxmox core/network, Docker DMZ, Technitium zones,
  firewall, OPNsense, Omada, routes, and certificate inventories. The PBS collector printed
  `no creds yet (/opt/skynet-ops/secrets/pbs.env) — collector idle until A2/A4` and exited cleanly.
- `scripts/envsync.sh` printed `skip aiometadata: no project.env on host` and
  `skip aiostreams: no project.env on host`; it changed no `.env.sops` file.
- No `~/.ssh/certs/` directory existed and `ssh-add -L` reported no authentication-agent
  connection. No root grant was active, no host root session occurred, and
  `inventory/grant-audit.json` was not written.
- `scripts/render-docs.sh` rendered the factual pages. The human narrative was rewritten from the
  collected state and the diff against `origin/main`. The deterministic digest and context-map
  renderers were run after this entry was filled so the digest could include this episode.

## Actions & outcomes

- Proxmox collection → both nodes online; all non-template guests listed in inventory running; VM
  9000 remains stopped; VM 9090 uptime advanced from 15,714 to 36,405 seconds versus `origin/main`.
- Docker collection → 18 containers observed; all `State=running` and all status strings contain
  `healthy`; identities and images unchanged versus `origin/main`.
- Certificate collection → 7 endpoints probed and 7 reachable.
- Omada collection → three devices connected, no upgrade flags; client counts 19 on Main Switch, 5
  on Mom's AP, 6 on Ali's AP (30 total versus 29 on `origin/main`).
- DNS collection → secondary zone SOA serial changed from `2026090100` to `2026090101`; collected
  zones showed no expiry, validation, sync, or notification failure.
- Firewall collection → counts changed from 41 aliases / 29 rules / 5 reservations on `origin/main`
  to 39 / 27 / 1. Removed objects were aliases `HOST_SMTP_RELAY` and
  `ROLE_IOT_ADMIN_TARGETS`, rules 330 and 350, and four mobile-device reservations.
  `ROLE_ADMIN_CLIENTS` lost `10.10.30.60`, `10.10.30.65`, and `10.10.30.55`;
  `ROLE_NFS_CLIENTS` lost `10.10.20.15` and `10.10.90.15`. `HOST_SKYNET_OPS` remained
  `10.10.90.90`, `ROLE_OPS_PRIV_TARGETS` remained empty, and rules 360–390 remained enabled.
- No T2 write, granted-root action, secret handling, infrastructure remediation, or PR merge ran.

## Graveyard — tried & abandoned

- `bin/new journal session "nightly 2026-09-01"` → refused because
  `journal/2026/2026-09-01-session-nightly-2026-09-01.md` already exists and episodes are
  append-only. Used the distinct title `nightly 2026-09-01 2350`.
- Running the nightly in the existing checkout → abandoned before collection because unrelated
  uncommitted changes were present. A separate worktree preserved those files untouched.

## Follow-ups / open threads

- Confirm whether the observed firewall removals and role-member reductions were intentional.
- PBS snapshot freshness, restic payloads, restore behavior, and the L5 Google Drive mirror remain
  unverified because the PBS credential was absent and no root grant was active.
