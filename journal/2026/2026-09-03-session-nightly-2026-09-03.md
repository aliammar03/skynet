---
date: 2026-09-03
kind: session          # session | incident | decision
title: nightly 2026-09-03
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [server-proxmox-core, server-proxmox-network, docker-dmz, OPNsense, Technitium, Omada]
---

# 2026-09-03 · session · nightly 2026-09-03

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened

At 03:38 PKT, fetched `origin/main` at `fe2c3cb3bcca8e7a614b9be06a6e7192caa9d14b`
and created `inventory/2026-09-03-0338` from it. Ran `bin/ops collect`,
`scripts/envsync.sh`, and `scripts/render-docs.sh` from `/home/aliammar/skynet`.

The collectors wrote inventories for both Proxmox nodes, `docker-dmz`, Technitium DNS,
OPNsense firewall and live state, Omada, routes, and seven TLS endpoints. The PBS collector printed
`no creds yet (/opt/skynet-ops/secrets/pbs.env) — collector idle until A2/A4`. Env sync printed
`skip aiometadata: no project.env on host` and `skip aiostreams: no project.env on host`; it did not
write an `.env.sops` file. No file matched `~/.ssh/certs/*-cert.pub`, so no root grant was active and
no sshd log was read or `inventory/grant-audit.json` written.

Against `origin/main`, Technitium's `aliammar.net` forwarder zone changed from wildcard
`*.aliammar.net A 10.10.100.35` to nine explicit A records for `aiometadata`, `aiostreams`, `auth`,
`calibre`, `karakeep`, `marinara`, `obsidian`, `sillytavern`, and `speed`, all pointing to
`10.10.100.35` and carrying `Managed by terraform`. Its SOA serial changed 12 to 22. The
`tofu-test.tdns.home.aliammar.net A 192.0.2.1` record remained present.

The Docker inventory contained 18 containers; all had state `running` and health `healthy`.
LibreSpeed changed container ID from `ee933c4df623` to `983d9d2a2458` and was created at
2026-09-03 01:26:23 PKT. Its image remained `ghcr.io/librespeed/speedtest:6.0.2-alpine` and its
Compose config hash remained `2e35672bfcf9092f86e5de65ee13978b764a4ba121452c2aa86e4297de3beece`.

Both Proxmox nodes answered. Every inventoried guest except stopped template VM 9000 was running.
OPNsense counts remained 39 aliases, 27 rules, and 1 reservation. ARP entries changed 41 to 39;
declared-host presence changed from 25 live / 1 no-response to 24 live / 2 no-response. The two
no-response addresses were `10.10.10.55` and Authentik `10.10.80.37`; CT 837 was running.
All three Omada devices reported up; reported clients changed main switch 20 to 16, Mom's AP 5 to
6, Ali's AP stayed 5. Seven of seven configured TLS probes were reachable.

Local CI-equivalent checks ran after rendering. `scripts/budget-frontmatter.sh --check` passed.
`scripts/check-invariants.sh` passed all four hard-law groups. `tests/entity-test.sh` printed
`entity-test: 44 passed, 1 failed`; the failed assertion was `vhosts.sql flags *.aliammar.net as a
front-door vhost — expected [caddy-apps], got []`. The test still names the wildcard removed from
live DNS. Because the command chain used `&&`, `tests/digest-test.sh` did not run in that chain; it
was then run separately and printed `digest-test: 5 passed, 0 failed`.

## Actions & outcomes

- `git fetch origin --prune` and branch creation → branch based exactly on latest `origin/main`.
- `bin/ops collect` → all configured non-PBS T1 collectors wrote fresh JSON; PBS stayed idle for
  absent credentials.
- `scripts/envsync.sh` → completed with two missing-`project.env` skips and no encrypted-env diff.
- root-grant presence check → no certificate present; grant audit harvest skipped.
- `scripts/render-docs.sh` → factual generated pages rendered at 2026-09-03T03:38:50+05:00.
- `docs/generated/05-state-of-the-lab.md` → rewritten from tonight's inventory and diff against
  `origin/main`.
- local deterministic checks → budget and hard invariants passed; entity derivation had one stale
  wildcard-vhost expectation; the digest test passed when run separately.
- `scripts/render-digest.sh` and `scripts/render-context-map.sh` → deterministic cold-boot digest
  and context map regenerated after the raw journal entry was filled.

## Graveyard — tried & abandoned

- A first `apply_patch` attempted `Delete File` and `Add File` for
  `docs/generated/05-state-of-the-lab.md` in one patch → rejected with `multiple operations target
  /home/aliammar/skynet/docs/generated/05-state-of-the-lab.md`; repeated as separate delete and add
  patches.

## Follow-ups / open threads

- Check whether Authentik `10.10.80.37` remains absent from ARP and ICMP on the next pass; CT 837
  was running during this collection.
- Confirm the intended lifetime of `tofu-test.tdns.home.aliammar.net → 192.0.2.1`.
- Confirm whether missing `project.env` files for `aiometadata` and `aiostreams` are intentional.
- PBS snapshot freshness, restic payloads, restore behavior, and the L5 Google Drive mirror remain
  unverified.
- Update the authored vhost derivation test/fixture to recognize the nine explicit
  `aliammar.net` app records instead of requiring `*.aliammar.net`; not changed in this report-only
  nightly branch.

<!-- Journal entries are APPEND-ONLY history: once written, an episode is not rewritten. A
     correction is a NEW entry that references this one, the same way git never edits a past
     commit. (journal/README.md) -->
