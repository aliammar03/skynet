---
date: 2026-09-03
kind: session
title: nightly 2026-09-03
tier_touched: [T1]
grants: []
refs: [SKY-002, SKY-018, vm-skynet-ops, vm-docker-dmz, server-proxmox-core, server-proxmox-network]
---

# 2026-09-03 · session · nightly 2026-09-03

## What happened

- Fetched `origin/main`, found the existing worktree clean, and created
  `inventory/2026-09-03-1143` directly from `origin/main`.
- Ran `bin/ops collect` at 11:44 PKT. Both Proxmox collectors, PBS, Docker, DNS, OPNsense,
  Omada, route, and TLS certificate collectors wrote inventory. The PBS collector reported one
  datastore and 111 snapshots. The certificate collector probed seven endpoints and reached all
  seven. `bin/ops collect` also invoked `scripts/render-docs.sh`.
- Ran `scripts/envsync.sh`. It printed `skip aiometadata: no project.env on host`, then
  `skip aiostreams: no project.env on host`, then exited 1. No `.env.sops` file changed.
- Ran `scripts/render-docs.sh` explicitly at 11:44:57 PKT.
- Checked `~/.ssh/certs` for `*-cert.pub`; no host certificate was present. No root connection
  was attempted, no grant was used, and `inventory/grant-audit.json` was not harvested.
- Compared collected inventory and rendered pages with `origin/main`. Guest identities and power
  states were unchanged. VMID 10015 (`vm-docker-dmz`) uptime changed from 404040 seconds to 38373
  seconds. Its 18 collected containers were running and healthy; container name/image pairs were
  unchanged.
- OPNsense counts changed from 41 to 39 ARP neighbours and from 25 live / 1 silent declared hosts
  to 24 live / 2 silent. `10.10.80.37` changed from ARP-present to `no-arp,no-icmp`.
- Omada reported the switch and both APs connected. Client counts changed from 20/5/5 to 20/4/4;
  switch remaining PoE changed from 108.2 W to 107.6 W.
- The DNS A-record set was unchanged from `origin/main`. The generated services page changed from
  its stale wildcard presentation to nine explicit `aliammar.net` application A records already
  present in the baseline inventory. The `aliammar.net` forwarder serial changed from 12 to 22 and
  the secondary serial from 2026090102 to 2026090300. The `tdns.home.aliammar.net` primary stayed
  at serial 291, and `tofu-test.tdns.home.aliammar.net` was absent in both compared inventories.
- PBS still reported 111 snapshots across 20 groups, with 11 groups lacking a verification state.
- Rewrote `docs/generated/05-state-of-the-lab.md` from those collected facts.

## Actions & outcomes

- `git fetch origin main --prune` → fetched `origin/main`; no local edits were present.
- `bin/ops collect` → all named collectors wrote output; render completed.
- `scripts/envsync.sh` → exited 1 after two missing-`project.env` messages; no encrypted env changed.
- `scripts/render-docs.sh` → regenerated factual pages at 11:44:57 PKT.
- Root-grant check → no certificate found; audit harvest skipped without using root.
- Inventory comparisons with `jq`, `comm`, and `git diff` → identified the VMID 10015 reboot
  boundary, `10.10.80.37` presence loss, DNS serial movement, and ordinary Omada telemetry changes.

## Graveyard — tried & abandoned

- First `scripts/envsync.sh` run stopped after the two missing-`project.env` messages. Re-ran it
  only to capture the exit status; it repeated the same messages and returned 1. No repair was
  attempted because this nightly is report-only.
- Initial `jq` projections used field names that do not exist in the Docker and PBS inventory
  schemas and returned null summaries. Inspected the actual top-level keys and repeated the reads
  with `Names`, `Image`, `State`, `Status`, `datastores[].store`, and `snapshot_total`.
- A full inventory diff was too noisy because API object order and volatile counters changed.
  Dropped that view and compared sorted identity/status projections plus the generated pages.

## Follow-ups / open threads

- Confirm whether VMID 10015's reboot was planned.
- Check `10.10.80.37` if the identity host should be continuously reachable.
- Confirm whether missing `project.env` files for `aiometadata` and `aiostreams` are intentional,
  and why `scripts/envsync.sh` exits 1 after those skips.
- Exercise a restore, verify restic and the L5 Google Drive mirror, and review the 11 PBS groups
  without a verification state.
