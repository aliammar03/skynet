---
date: 2026-08-27
kind: session          # session | incident | decision
title: nightly 2026-08-27
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [runbooks/nightly.md, "inventory/2026-08-27"]
---

# 2026-08-27 · session · nightly 2026-08-27

## What happened
Ran the agent-path nightly (Claude, manual invocation, not the systemd timer) per
`runbooks/nightly.md`. Session started on branch `phase/sky-008-p2-throwaway-guest` with
uncommitted work in progress: `docs/design/access-and-trust.md` modified (+31/-2) and two
untracked files `tofu/template-ubuntu-2404.tf`, `tofu/throwaway-guest.tf` (SKY-008 P2). Stashed
that WIP (`git stash push -u -m "sky-008-p2 wip before nightly"`) before touching branches so the
nightly wouldn't mix with it, then checked out a fresh `inventory/2026-08-27` off `origin/main`
(HEAD `8194903`).

`bin/ops collect` ran all five collectors: proxmox-core, proxmox-network, docker-docker-dmz,
dns-zones, firewall. PBS collector stayed idle (no creds at `/opt/skynet-ops/secrets/pbs.env`,
as expected — A2/A4 territory). `scripts/envsync.sh` found no `project.env` changes on any of
the 10 known projects — "no env changes". `scripts/render-docs.sh`, `scripts/render-digest.sh`,
`scripts/render-context-map.sh` all ran clean.

No active root grant this session — `ssh-add -l` returned "Could not open a connection to your
authentication agent" and `~/.ssh/id_ed25519` on this box is the agent-owned Nix-managed key
(`/run/secrets/agent-ssh-key`), not a signed user-CA cert. Skipped the grant-audit step per
runbook step 5 ("skip if no root").

## Actions & outcomes
- `git stash push -u` on `phase/sky-008-p2-throwaway-guest` → stashed SKY-008 P2 WIP cleanly (`stash@{0}`), working tree clean.
- `git checkout -B inventory/2026-08-27 origin/main` → fresh branch off `8194903`, clean tree.
- `./bin/ops collect` → wrote `inventory/proxmox-core.json`, `proxmox-network.json`, `docker-docker-dmz.json`, `dns-zones.json`, `firewall/firewall.json` (aliases 39→41, rules=29, reservations=6).
- `./scripts/envsync.sh` → no changes (all 10 projects skip: "no project.env on host" — expected, secrets live on vm-docker-dmz not here).
- `./scripts/render-docs.sh`, `render-digest.sh`, `render-context-map.sh` → all wrote successfully, diffs reviewed below.
- Diff vs main (14 files, +587/-515): last render was stamped 2026-08-20T10:52:41Z — a full week stale, so this catches 7 days of drift, not one night's worth.
  - **Proxmox core**: VMID 999 (legacy `vm-skynet-ops`, qemu) running→**stopped**. VMID 9090 `vm-skynet-ops` now tagged pool `ops-managed` (was untagged) and stays running. New VMID 9091 `vm-skynet-ops-nix` appears, **stopped**, pool `ops-managed`. This matches the open thread from the 2026-08-26 SKY-007 cutover episode ("Renumber VMID 9091 → 9090 on next boot") — reads as *done*: 9090 is now the live NixOS host, 9091 is the stopped source left behind, 999 is the old pre-NixOS guest now retired. Not verified beyond the inventory snapshot — flagging, not confirming.
  - **Proxmox network**: LXC 1033 `lxc-cloudflared` stopped→**running** (SKY-014's tunnel container — this is the expected/healthy state, was down at last observation).
  - **DNS**: zone `soaSerial` and `lastModified`/`lastUsedOn` timestamps advanced normally (SOA 2026082000→2026082602 on the reverse zone, 222→265 on `tdns.home.aliammar.net`) — routine resolver activity, nothing anomalous.
  - **Firewall (OPNsense mirror, T1 read-only — these are Ali's or an earlier session's edits, not this run's)**: `HOST_SKYNET_OPS` alias content changed `10.10.90.99,10.10.90.90` → `10.10.90.90,10.10.90.91` (the `.91` address is new — matches the new VMID 9091 guest's likely IP, `.99` dropped). Rule 210 (admin clients → mgmt proxy) gained `HOST_SKYNET_OPS` as an explicit source. Rule 300 (Unraid SMB) now targets a new alias `PORT_SMB_NFS` (445,2049) instead of hardcoded `445`, description updated to "SMB/NFS". Rule 830 (Caddy → DNS/ACME egress) gained `HOST_PROXY_APPS` as a source. New alias `HOST_OMADA` (10.10.50.25) added to `ROLE_ADMIN_TARGETS`. `PORT_WEB` alias description shortened from "Standard web ports: HTTP 80 and HTTPS 443." to "Ports for WEB". Alias count 39→41.
  - **Docs**: all 10 generated pages picked up the new `generated:` timestamp + the above facts; no rendering errors.

## Graveyard — tried & abandoned
— nothing abandoned (a clean pass; every step succeeded) —

## Follow-ups / open threads
- Confirm the VMID 9091→9090 renumber (SKY-007 follow-up) is actually done and intentional — inventory now shows 9090 running/ops-managed + 9091 stopped/ops-managed, consistent with "done", but this session only read the mirror, didn't verify on the node.
- `PORT_WEB` alias description regressed to a generic placeholder ("Ports for WEB") — looks like an unintentional overwrite of the more descriptive original text; worth a one-line fix in OPNsense if Ali agrees.
- HOST_SKYNET_OPS gaining direct access to the management proxy (rule 210) is a live T3 change outside this session's scope — surfacing it here since it widens agent-adjacent reach; not acted on, just noted per the "any anomaly → flag, don't fix silently" guardrail.
- SKY-008 P2 throwaway-guest WIP (`tofu/template-ubuntu-2404.tf`, `tofu/throwaway-guest.tf`, `docs/design/access-and-trust.md` edits) is stashed (`stash@{0}` on `phase/sky-008-p2-throwaway-guest`) — restore with `git checkout phase/sky-008-p2-throwaway-guest && git stash pop` next session.
