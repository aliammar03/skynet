---
date: 2026-09-01
kind: session          # session | incident | decision
title: nightly 2026-09-01
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [server-proxmox-core, server-proxmox-network, vm-docker-dmz]
---

# 2026-09-01 · session · nightly 2026-09-01

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened

- Created branch `inventory/2026-09-01-0343` in an isolated worktree from fetched
  `origin/main` commit `9676826c90bd29f06a3c12d563605d0d6180e1e5`.
- Ran `bin/ops collect` at 03:44 PKT. The core and network Proxmox collectors, Docker collector,
  DNS collector, and firewall-mirror collector wrote fresh inventory. The firewall mirror reported
  HEAD `aba7911` dated 2026-08-26 and rendered 41 aliases, 29 rules, and 6 reservations.
- `collect-pbs.sh` printed `no creds yet (/opt/skynet-ops/secrets/pbs.env) — collector idle until
  A2/A4`; it did not produce live PBS snapshot evidence.
- Ran `scripts/envsync.sh`. For each tracked project (`aiometadata`, `aiostreams`, `caddy-apps`,
  `calibre`, `cloudflared`, `karakeep`, `librespeed`, `marinara`, `obsidian-livesync`, and `silly`),
  it printed `no project.env on host`; final output was `no env changes`.
- Checked `$HOME/.ssh/certs` before grant-audit harvesting. The directory did not exist, so there
  was no active local root certificate and no host sshd logs were queried.
- Ran `scripts/render-docs.sh`, `scripts/render-digest.sh`, and
  `scripts/render-context-map.sh`. Rewrote `docs/generated/05-state-of-the-lab.md` from the current
  inventory and the diff against `origin/main`.
- Compared Proxmox resources, Docker containers/images, DNS records, and firewall counts with
  `origin/main`. Both Proxmox guest sets and all guest power states matched. Core CT 240 was
  running; network CTs 720 and 1035 were stopped. Docker reported the same 18 containers, all
  running and healthy. DNS record tuples and firewall counts matched.
- The remaining diff contained collection timestamps, uptime and live resource counters, DNS
  expiry/use metadata, Docker mount/network ordering, Calibre writable-size movement, humanized
  image-age text, and regenerated-page timestamps.

## Actions & outcomes

- Inventory refresh → five inventory JSON files and the firewall JSON refreshed successfully.
- PBS collection → skipped because `/opt/skynet-ops/secrets/pbs.env` was absent.
- Environment sync → no remote `project.env` was present; no `.env.sops` file changed.
- Root-grant audit → skipped because no local certificate directory existed; `grants` remains empty.
- Documentation render → factual pages, agent digest, context map, and human narrative refreshed.
- Operational scope → no guest, Docker, DNS, firewall, service, root, or credential write was run.

## Graveyard — tried & abandoned

- Live PBS freshness collection → collector stopped at its credential guard because
  `/opt/skynet-ops/secrets/pbs.env` was absent; no alternate or privileged path was attempted.
- Root-grant sshd-log harvest → not started because `$HOME/.ssh/certs` did not exist; no root
  access was requested.

## Follow-ups / open threads

- Recent PBS snapshots and the L5 Google Drive mirror remain unverified by this run.
- Confirm that core VMIDs 101, 231, 999, and 9091, absent since the August 31 collection, were
  intentionally removed.
- Resolve ownership of `10.10.100.35` before any destruction of stopped CT 1035.
- CT 526 remains running and unmapped in DNS/reservations.

<!-- Journal entries are APPEND-ONLY history: once written, an episode is not rewritten. A
     correction is a NEW entry that references this one, the same way git never edits a past
     commit. (journal/README.md) -->
