---
date: 2026-08-31
kind: session          # session | incident | decision
title: nightly 2026-08-31
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [server-proxmox-core, server-proxmox-network, docker-dmz, inventory/2026-08-31-0334]
---

# 2026-08-31 · session · nightly 2026-08-31

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened
At 03:35 PKT, ran `bin/ops collect` from branch `inventory/2026-08-31-0334`, created from
`origin/main` commit `3e96a76`. The collectors queried both Proxmox nodes, `docker-dmz`,
Technitium zones, and the firewall git mirror. The PBS collector printed
`no creds yet (/opt/skynet-ops/secrets/pbs.env) — collector idle until A2/A4`.

Ran `scripts/envsync.sh`. It printed `skip <project>: no project.env on host` for aiometadata,
aiostreams, caddy-apps, calibre, cloudflared, karakeep, librespeed, marinara,
obsidian-livesync, and silly, then `no env changes`.

`ssh-add -L` printed `Could not open a connection to your authentication agent.` No user SSH
certificate was present in `~/.ssh`, so no root grant was active and no sshd-log/grant-audit read
was attempted.

Against `origin/main`, the core Proxmox inventory no longer listed CT 101 (`debian`), stopped CT
231 (`lxc-adguard-core`), VM 999 (`vm-skynet-ops`), or stopped VM 9091
(`vm-skynet-ops-nix`). The other core guests retained their states. The network-node guest list
and states did not change.

Docker returned 18 containers, all running and healthy. No container name or image changed.
`aiometadata-aiometadata-1` and `aiometadata-redis-1` reported 13 hours uptime versus 46 hours in
the main snapshot. The other 16 containers reported two days uptime. The firewall mirror stayed
at `aba7911` with 41 aliases, 29 rules, and 6 reservations. `pbs-unraid` reported available on
both Proxmox nodes.

## Actions & outcomes
- `bin/ops collect` → rewrote five inventory sources and factual generated pages.
- `scripts/envsync.sh` → no `.env.sops` changes.
- Root-grant check → no active grant; no sshd-log audit was attempted.
- Rendered factual docs, rewrote the human state page, and regenerated the deterministic digest
  and context map after writing this entry.

## Graveyard — tried & abandoned
Negative results are memory too. Anything attempted that did NOT work — and *why* — so a future
cold agent doesn't re-walk the dead end. Leave a single "— nothing abandoned —" line only if the
episode genuinely tried no path it dropped.

- Queried Docker records using lowercase fields (`name`, `state`, `health`) → abandoned after the
  query returned nulls; the collector uses `Names`, `State`, `HealthStatus`, and `Status`.

## Follow-ups / open threads
- Confirm whether removal of core VMIDs 101, 231, 999, and 9091 was intentional.
- PBS/restic snapshot freshness remains unverified because the PBS credential is absent and no
  root grant was active.
- Ownership of `10.10.100.35` still needs resolution before stopped CT 1035 is destroyed.
- CT 526 remains running and unmapped in DNS/reservations.

<!-- Journal entries are APPEND-ONLY history: once written, an episode is not rewritten. A
     correction is a NEW entry that references this one, the same way git never edits a past
     commit. (journal/README.md) -->
