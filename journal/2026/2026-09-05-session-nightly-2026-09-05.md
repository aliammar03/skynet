---
date: 2026-09-05
kind: session
title: nightly 2026-09-05
tier_touched: [T1]
grants: []
refs: [server-proxmox-core, server-proxmox-network, vm-docker-dmz, lxc-athena, lxc-authentik]
---

# 2026-09-05 · session · nightly 2026-09-05

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened

Fetched `origin/main` at commit `7197a74bab32735eda94651ca5b2b1b739113efa` and created
`inventory/2026-09-05-0341` from it. The starting worktree on
`phase/sky-athena-own-repo` was clean.

Ran `bin/ops collect`. It wrote the Proxmox core/network resource and ACL inventories, PBS,
`docker-dmz`, DNS zones, firewall, OPNsense live state, Omada network gear, routes, and TLS
certificate probes. Collection completed at 2026-09-05 03:42 PKT. Both Proxmox nodes answered.
All non-template guests were running. The Docker inventory contained 18 containers; every entry
had `State: running` and `HealthStatus: healthy`. Certificate collection probed seven endpoints and
all seven were reachable. Omada returned the main switch and two APs with `connected: true`.

Ran `scripts/envsync.sh`. It printed `skip aiometadata: no project.env on host` and
`skip aiostreams: no project.env on host`, then exited 1. No `compose/*/.env.sops` file changed.

Checked the documented local grant directory, `~/.ssh/certs`, for per-host `*-cert.pub` files.
No certificate was present, so no root grant was active. No host root login was attempted and
`inventory/grant-audit.json` was not harvested or changed.

Ran `scripts/render-docs.sh`, `scripts/render-digest.sh`, and
`scripts/render-context-map.sh`. Rewrote `docs/generated/05-state-of-the-lab.md` from the collected
facts and the diff against `origin/main`. The deterministic renderers updated the factual pages,
agent digest, and context map.

Against `origin/main`, PBS moved from 122 snapshots in 20 groups to 134 snapshots in 21 groups.
Core CT 10030 (`lxc-athena`) acquired its first PBS snapshot. Eleven existing daily backup groups
each gained one snapshot. The core 03:30 and network 02:00 backup jobs both reported `OK` on
2026-09-05. The 11 latest snapshots already lacking a verification state in `origin/main` still
lacked one; CT 10030 added a twelfth unverified latest snapshot.

Proxmox guest identities and power states did not change against `origin/main`. Docker container
IDs, images, running state, and health did not change. OPNsense declared-host presence moved from
26 live / 1 silent to 25 live / 2 silent: `10.10.80.37` changed from ARP-present to
`no-arp,no-icmp`; `10.10.10.55` remained silent. OPNsense live ARP neighbours moved from 48 to 40;
interfaces remained 17. Firewall inventory remained 40 aliases, 27 rules, and 1 reservation.
Omada device connection and firmware state did not change; main-switch clients moved 25 to 18,
Mom's AP clients 7 to 5, Ali's AP clients 6 to 7, and switch PoE remaining moved 106.8 W to
108.2 W. DNS remained four zones/four records, route inventory remained nine routes, and TLS
reachability remained 7/7.

## Actions & outcomes

- `git fetch origin main` and branch creation → branch based on the latest `origin/main`.
- `bin/ops collect` → all configured T1 collectors wrote fresh inventory.
- `scripts/envsync.sh` → skipped two projects without `project.env`, exited 1, changed no encrypted env file.
- root-grant check → no active certificate found; audit harvest skipped without contacting hosts as root.
- deterministic render commands → generated factual docs, cold-boot digest, and context map refreshed.
- narrative render → human state page rewritten with the `origin/main` comparison and anomalies.

## Graveyard — tried & abandoned

- A single `apply_patch` containing both `Delete File` and `Add File` for
  `docs/generated/05-state-of-the-lab.md` → rejected with `multiple operations target` for the same
  path; used two sequential `apply_patch` operations instead.
- The first shell loop for baseline JSON summaries constructed `git show origin/main:` with a space
  before each path → `jq` received git's error text and returned `parse error: Invalid literal at
  line 1, column 5`; reran with `git show "origin/main:<path>"`.

## Follow-ups / open threads

- Confirm that the 12 newest PBS snapshots acquire `verify_state: ok`; CT 10030 is a new backup
  group and the other 11 latest snapshots were already unverified in the `origin/main` baseline.
- Exercise an end-to-end restore; this run read backup metadata only.
- Decide whether missing `project.env` for `aiometadata` and `aiostreams` is intentional and whether
  `scripts/envsync.sh` should exit 1 after those skips.
- Confirm whether `10.10.80.37` should answer continuously. CT 837 was running during this pass,
  while the separate identity address returned neither ARP nor ICMP presence.

<!-- Journal entries are APPEND-ONLY history: once written, an episode is not rewritten. A
     correction is a NEW entry that references this one, the same way git never edits a past
     commit. (journal/README.md) -->
