---
date: 2026-08-29
kind: session          # session | incident | decision
title: nightly 2026-08-29
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [runbooks/nightly.md, "inventory/2026-08-29", SKY-008]
---

# 2026-08-29 · session · nightly 2026-08-29

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened
Ran the agent-path nightly (Claude, manual invocation) per `runbooks/nightly.md`, on branch
`main`, HEAD `e3be142`, clean tree except one pre-existing untracked file
(`journal/2026/2026-08-28-session-seed-jikan-anime-index-for-aiometadata.md` — someone's prior
session note, left alone, not part of this run).

`bin/ops collect` ran all five collectors: proxmox-core, proxmox-network, docker-docker-dmz,
dns-zones, firewall. PBS collector stayed idle (no creds at `/opt/skynet-ops/secrets/pbs.env`,
expected — A2/A4 territory). `scripts/envsync.sh` found no `project.env` changes on any of the
10 known projects. `scripts/render-docs.sh`, `render-digest.sh`, `render-context-map.sh` all ran
clean, stamped `2026-08-29T03:30:5x+05:00`.

No active root grant: `~/.ssh/certs/` doesn't exist on this box, no `/home/ali/.config/skynet-ops/`
either. Skipped the grant-audit step per runbook step 5.

Diff vs `main` (15 files, +540/-537 lines) — last render was 2026-08-27, so this is ~2 nights of
drift:

- **Proxmox core (`inventory/proxmox-core.json`)**:
  - VMID **240** `lxc-proxmox-backup-server` (pool `ops-managed`) flipped **running → stopped**.
    This is the PBS datastore host backing the L5 `PBS → Google Drive` backup layer
    (`docs/generated/90-backup-status.md`, nightly 04:00 upload). Nothing in git history or
    `journal/2026-08-2[6-8]-*` explains this stop — not part of any SKY-008 session. Flagging as
    an anomaly per the "any anomaly → flag, don't fix silently" guardrail; did not touch it (T2
    write / node-console action, out of nightly's report-only scope anyway).
  - VMID **999** (legacy `vm-skynet-ops`, pre-NixOS, untagged/no pool) flipped **stopped →
    running**. The 2026-08-27 nightly episode had this at "stopped" and read the SKY-007 renumber
    as done (999 retired). It being running again is unexplained by any journal entry between
    then and now — flagging, not acting (not `ops-managed`, out of write scope regardless).
  - VMID **101** `debian` (lxc, untagged) flipped **stopped → running** — minor, not
    `ops-managed`, likely Ali poking at it directly; noting for completeness only.
  - VMID **9000** `ubuntu-2404-base` (qemu, `ops-managed`) is new, **stopped** — this is expected:
    the permanent Tofu base template built in the 2026-08-27 SKY-008 P2 session
    (`journal/2026-08-27-session-sky-008-p2-tofu-vm-lifecycle-round-trip-on-core-node.md`). Kept
    stopped by design (a clone source, not a running guest).
- **Proxmox network (`inventory/proxmox-network.json`)**: LXC **1033** `lxc-cloudflared`
  disappeared from the resource list entirely (was running as of 2026-08-27's nightly). Fully
  explained: the 2026-08-27 SKY-008 network-node session
  (`journal/2026-08-27-session-sky-008-tofu-extended-to-network-node-lxc-clone-round-trip.md`)
  used 1033 as the Tofu lifecycle proof — cloned it to 1099, then destroyed *both* 1033 and 1099
  on Ali's "destroy them both". Not an anomaly; matches the record. (Note: the docker-dmz host
  still runs its own `cloudflared` container for the actual tunnel — see below — so the public
  path was never dependent on the now-gone LXC.)
- **Docker (`inventory/docker-docker-dmz.json`)**: same 18 containers, no adds/removes. Every
  container's `Status` reset to `Up 11 hours` (was `Up 3–11 days` per-container, staggered) —
  all 18 restarted at the same point in time, ~11h before this collect (~2026-08-28T16:30
  +05:00-ish). Reads as a `vm-docker-dmz` host reboot or `docker compose`/daemon restart across
  the board, not per-service redeploys (no image/size churn beyond routine layer diffs). Not
  explained by any journal entry in the window — flagging as an anomaly to confirm with Ali, not
  acted on (T1 read-only observation).
- **DNS (`inventory/dns-zones.json`)**: routine SOA serial advances (reverse zone
  2026082602→2026082801, `tdns.home.aliammar.net` 265→286) and DNSSEC re-signing
  (signatureExpiration rolled 2026-09-08→2026-09-11) — normal resolver housekeeping, nothing
  anomalous.
- **Firewall (`inventory/firewall/firewall.json`)**: timestamp-only diff — no alias/rule content
  changed since 2026-08-27 (41 aliases, 29 rules, 6 reservations, all stable).

## Actions & outcomes
- `bin/ops collect` → refreshed all 5 inventory files + ran `render-docs.sh` once inline.
- `./scripts/envsync.sh` → "no env changes" (all 10 projects skip: no `project.env` on this host).
- `./scripts/render-docs.sh` (explicit re-run), `render-digest.sh`, `render-context-map.sh` → all
  wrote clean, no errors.
- Diffed `proxmox-core.json` / `proxmox-network.json` / `docker-docker-dmz.json` resource-by-resource
  (python3 one-liners) against `git show HEAD:...` to isolate real state changes from timestamp
  noise — found the 240/999/101/9000/1033 changes above precisely (not just eyeballing the
  rendered markdown diff).
- Cross-checked against `journal/2026-08-2[6-8]-*` and `planning/projects/SKY-008-*.md` to
  separate "expected, already explained" (9000 added, 1033 removed) from "unexplained, flag it"
  (240 stopped, 999 running, docker-dmz uptime reset).
- Rewrote `docs/generated/05-state-of-the-lab.md` narrative with tonight's findings.
- No active root grant → skipped grant-audit step (runbook step 5).

## Graveyard — tried & abandoned
— nothing abandoned (a clean pass; every step succeeded) —

## Follow-ups / open threads
- **PBS (VMID 240) is stopped** — confirm with Ali whether this is deliberate maintenance or an
  unplanned outage. If unplanned, the L5 PBS→Drive nightly upload (04:00) has nothing to mirror
  until it's back up. Not acted on — starting/stopping guests is outside nightly's report-only
  scope and 240, while `ops-managed`, wasn't touched by any tofu session this window.
- **VMID 999 (legacy pre-NixOS `vm-skynet-ops`) is running again** — the 2026-08-27 nightly read
  its "stopped" state as confirmation the SKY-007 VMID renumber (9091→9090) had landed cleanly.
  It coming back up is unexplained; worth confirming it's intentional (e.g. Ali booting it to
  compare/verify something) rather than a stray duplicate of the live ops box.
- **`vm-docker-dmz`'s containers all show `Up 11 hours`** as of this collect — points to a host
  reboot or full `docker compose`/daemon restart roughly 2026-08-28 ~16:30 +05, not individual
  service redeploys. No corresponding journal entry found; worth asking Ali if this was
  deliberate (e.g. a host update) or worth investigating.
- SKY-008 P3 (DNS provider + declarative LXC import) is still not started (per
  `planning/projects/SKY-008-*.md` status log, last entry 2026-08-27).
