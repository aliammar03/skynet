---
date: 2026-09-03
kind: session          # session | incident | decision
title: nightly 2026-09-03 1206
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [server-proxmox-core, server-proxmox-network, docker-dmz, OPNsense, PBS, Omada]
---

# 2026-09-03 · session · nightly 2026-09-03 1206

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened

- Fetched `origin` and created `inventory/2026-09-03-1206` at
  `c5ada36ad138c78eee89ba1f7aaea123b98e7d63`, the then-current `origin/main`.
- Ran `bin/ops collect`. It wrote the core and network Proxmox inventories, PBS inventory,
  `docker-docker-dmz.json`, DNS zones, firewall configuration, OPNsense live state, Omada network
  gear, routes, and seven TLS probes. Collection completed without a collector error.
- The collection timestamp was 12:06:59–12:07:07 PKT. Both Proxmox nodes were online. Every
  non-template guest was running. All 18 Docker containers had `State: running` and
  `HealthStatus: healthy`. All seven certificate probes were reachable. All three Omada devices
  had status 1.
- Compared with `origin/main`, PBS still had 111 snapshots across 20 groups, but latest-snapshot
  `verify_state` changed from null to `ok` for core CTs 731 and 751; core VMs 9000 and 9090;
  network CTs 525, 635, 730, 750, and 837; and network VM 5001. The PBS `unverified` count changed
  from 11 to 1. Core VM 10015 remained null.
- Compared with `origin/main`, OPNsense ARP entries changed from 39 to 37. Declared presence stayed
  at 24 live and 2 silent; `10.10.10.55` and `10.10.80.37` were still `no-arp,no-icmp`.
  `10.10.100.65` was live via ICMP instead of ARP. Firewall configuration counts stayed at 39
  aliases, 27 rules, and 1 reservation.
- Compared with `origin/main`, the Omada main-switch client count changed from 20 to 24, both APs
  stayed at 4, and remaining switch PoE changed from 107.6 W to 107.7 W.
- No file matching `~/.ssh/certs/*-cert.pub` existed. No root grant was active; no host root login
  was attempted and `inventory/grant-audit.json` was not harvested.
- Ran `scripts/render-docs.sh`, `scripts/render-digest.sh`, and
  `scripts/render-context-map.sh`. Rewrote `docs/generated/05-state-of-the-lab.md` from the live
  inventory and the diff against `origin/main`.

## Actions & outcomes

- `bin/ops collect` → completed; all configured inventory surfaces wrote fresh JSON.
- `scripts/envsync.sh` → printed `skip aiometadata: no project.env on host` and
  `skip aiostreams: no project.env on host`, exited 1, and changed no `.env.sops` file.
- `scripts/render-docs.sh` → regenerated factual pages from inventory.
- `scripts/render-digest.sh` → regenerated the deterministic cold-boot digest.
- `scripts/render-context-map.sh` → regenerated the deterministic context map.
- Root-grant check → no active certificate found; grant audit skipped.

## Graveyard — tried & abandoned

- `bin/new journal session "nightly 2026-09-03"` → exited 1 because
  `journal/2026/2026-09-03-session-nightly-2026-09-03.md` already existed. Did not overwrite the
  append-only episode; created this distinct `nightly 2026-09-03 1206` entry instead.

## Follow-ups / open threads

- VM 10015 is the only PBS guest group whose latest snapshot has no verification state.
- No restore was exercised; PBS verification does not prove the end-to-end restore path.
- Confirm whether missing `project.env` files for `aiometadata` and `aiostreams` are intentional;
  `scripts/envsync.sh` continues to exit 1 after those skips.
- Confirm whether `10.10.10.55` and `10.10.80.37` are expected to answer continuously.

<!-- Journal entries are APPEND-ONLY history: once written, an episode is not rewritten. A
     correction is a NEW entry that references this one, the same way git never edits a past
     commit. (journal/README.md) -->
