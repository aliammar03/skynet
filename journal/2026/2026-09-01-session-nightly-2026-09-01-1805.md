---
date: 2026-09-01
kind: session          # session | incident | decision
title: nightly 2026-09-01 1805
tier_touched: [T1]      # tiers this episode ACTUALLY used (not what it could touch)
grants: []              # root grants used this episode: "host KeyID", else empty
refs: [server-proxmox-core, server-proxmox-network, docker-dmz, vm-skynet-ops, SKY-018]
---

# 2026-09-01 · session · nightly 2026-09-01 1805

<!-- RAW EPISODE. Write what actually happened, in the concrete. Do NOT summarize, generalize,
     or collapse this into a lesson — that destroys the episodic signal before it can be used
     (SKY-006 / journal/README.md). Distillation happens at READ time, never here. -->

## What happened
At 18:06 PKT, ran the report-only nightly from a worktree at `origin/main` commit
`63887204ada8523030c0107a8eb471b6034ec492` on branch `inventory/2026-09-01-1805`.
`bin/ops collect` queried both Proxmox nodes, docker-dmz, Technitium DNS, the OPNsense git mirror,
and the Omada controller. The PBS collector printed `no creds yet
(/opt/skynet-ops/secrets/pbs.env) — collector idle until A2/A4`. The firewall collector read mirror
commit `a610af0` and reported 41 aliases, 29 rules, and 6 reservations. The Omada collector reported
three connected devices.

Compared with `origin/main`'s approximately 17:36 PKT inventory, guest identities and power states
did not change. All 18 Docker containers remained running and healthy. Omada's total stayed at 29
clients: Mom's AP changed from 4 to 5, Ali's AP from 6 to 5, and the switch remained at 19. Switch
free PoE changed from 107.2 W to 108.2 W. DNS observation timestamps, uptime/resource counters, and
Proxmox JSON field order changed. No infrastructure write was performed.

## Actions & outcomes
- `bin/ops collect` → refreshed six inventory sources and rendered an initial generated-doc pass.
- `scripts/envsync.sh` → skipped aiometadata and aiostreams because neither host `project.env` was
  present; no `.env.sops` file changed.
- Checked `$HOME/.ssh/certs` → directory absent; no active grant and no grant audit to harvest.
- `scripts/render-docs.sh` → regenerated factual pages from the 18:06 inventory.
- Rewrote `docs/generated/05-state-of-the-lab.md` → recorded the stable half-hour diff and the
  unverified backup boundary without claiming restore evidence.

## Graveyard — tried & abandoned
- `bin/new journal session "nightly 2026-09-01"` → refused because
  `journal/2026/2026-09-01-session-nightly-2026-09-01.md` already exists and journal episodes are
  append-only; used the distinct title `nightly 2026-09-01 1805`.

## Follow-ups / open threads
- PBS snapshot freshness, restic payloads, and the L5 Google Drive mirror were not verified because
  the PBS credential was absent and no root grant was active.
- OPNsense T3 cleanup remains human-owned: remove stale `10.10.90.91` from `HOST_SKYNET_OPS` and
  optionally remove the old downstairs-AP reservation.

<!-- Journal entries are APPEND-ONLY history: once written, an episode is not rewritten. A
     correction is a NEW entry that references this one, the same way git never edits a past
     commit. (journal/README.md) -->
