[Unit]
Description=Weekly skynet CLI update + model-suggestion refresh

[Timer]
# Sunday 05:00 — after the nightly (03:30) and the PBS sync (04:00).
OnCalendar=Sun *-*-* 05:00:00
RandomizedDelaySec=30m
Persistent=true

[Install]
WantedBy=timers.target
