# NON-secret config — committed plaintext, Arcane's git layer. Secrets live in .env.sops.
# CouchDB admin username = the login you enter in the Obsidian LiveSync plugin.
# The password and the session secret are in .env.sops.
COUCHDB_USER=aliammar
