# NON-secret config — committed plaintext. Secrets live in .env.sops.
# calibre is configured entirely via the compose `environment:` block and has no secrets,
# so this layer is intentionally empty. It exists so `env_file: .env` always resolves
# (`skynet deploy` renders .env = .env.git + decrypt(.env.sops) in tmpfs).
