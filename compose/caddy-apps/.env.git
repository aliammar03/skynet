# NON-secret config — committed plaintext, Arcane git layer. Secrets live in .env.sops.
# The sole secret is CLOUDFLARE_API_TOKEN (scoped Zone→DNS→Edit for aliammar.net only),
# which the Caddyfile reads as {env.CLOUDFLARE_API_TOKEN} — it lives ONLY in .env.sops.
ACME_EMAIL=aliammar03@gmail.com
